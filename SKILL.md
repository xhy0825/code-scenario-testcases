---
name: code-scenario-testcases
description: 根据业务代码生成研发自测用例 Excel 表格：把业务代码梳理成 业务场景 → 功能 → if/else/switch 条件分支，每条用例含前置条件、操作步骤、预期结果（不通过分支写出功能展示的错误提示）与覆盖率说明。当用户要求"根据代码生成测试用例/自测用例/用例清单"、"梳理业务场景和功能点"、"把某段代码的 if/else/switch 分支整理成测试用例"、"对代码做分支覆盖式自测"、或给出一个代码目录/文件并想要用例表时，使用本 skill。支持命令式指令 `code-scenario-testcases -all`（全量扫描）、`-diff`（当前未提交变更）、`-diff [commitId]`（指定提交之后变更）。语言无关，支持 Java/Go/Python/JS/TS/C#/C++/Rust。
---

# 从代码逻辑生成研发自测用例

## 定位与角色

读代码 → 反推 **业务场景 → 功能 → 条件分支用例** 三级结构，输出 Excel 自测用例表。以**资深研发自测架构师**身份执行，自检准则：每条用例可执行（前置可构造、步骤可复现、预期可判定）、高风险优先（金额/权限/状态迁移 → P0）、诚实交付（不粉饰边界）、报告专业（统计+覆盖率+缺口+风险）。

## 核心原则

1. **忠实代码**：分支来自真实代码，不脑补；错误串原样摘录进预期结果
2. **机器基准**：判定点覆盖分母 = `scripts/enumerate_decision_points.py` 枚举的真实判定点，漏测亮红灯
3. **不硬凑**：需求未实现/外部依赖/框架隐含三类无法从静态代码对应 → 显式标注（需求未实现场景 / 推断需联调 / 无法静态验证）

## 输入

- 目录（递归扫描）、单个文件/函数、或粘贴的代码片段
- **可选：需求/契约注入**——附需求描述、接口契约、验收标准，预期结果优先对照需求、并触发第 5 步规则缺口审查
- 未指定范围时默认全目录扫描

## 调用方式

| 指令 | 说明 |
|---|---|
| `code-scenario-testcases -all` | 全量扫描整个目录（默认） |
| `code-scenario-testcases -diff` | 只分析未提交变更（`git diff HEAD` + 未跟踪新文件） |
| `code-scenario-testcases -diff [commitId]` | 分析该提交之后到当前（`git diff commitId`） |
| `code-scenario-testcases -help` | 输出基本使用帮助并结束（不执行扫描） |

- **检测到 `-help` 或用户询问"怎么用/有哪些指令"时**：直接输出 `references/help.md` 的内容并结束，不执行扫描流程
- 非 git 仓库或无变更 → 回退全量扫描并说明
- 定位**受影响函数**（变更行所在函数体），以整函数为单位生成完整分支用例（防回归）；`-diff` 额外 grep 变更函数调用方标记"间接受影响"

## 工作流程

### 第 1 步 扫描过滤
递归收集源码，按扩展名识别语言（见 `references/language-guide.md`）；**跳过构建产物/依赖目录/测试文件**。

- **依赖库与 SDK 不生成用例**：第三方依赖（jar / node_modules / vendor 等）的内部实现不是被测对象，一律排除；只测**项目自身的业务代码**
- **对依赖的调用判定点仍测**：项目代码里 `if (!stock.check(...))` 这类"调用依赖后的分支"属于业务逻辑，会生成用例——测的是"你的代码如何处理依赖返回"，不是"依赖内部实现"；这类用例预期结果标注 `推断（需联调）` 或 `无法静态验证`

### 第 2 步 识别场景与功能
入口方法 = 功能（HTTP 路由/注解/导出函数）；相关功能聚为业务场景（同 Controller/同实体/同模块）。产出**功能入口清单**（已分析 or 显式排除+原因），作为覆盖率分母。

### 第 3 步 提取分支生成用例
对每个功能（保持映射意识：功能 = 代码入口，每条用例 = 一条条件路径，预期结果 = 该路径"看到"的现象；对不上就重写或标注）：

1. **判定点 = 测试单元**：`if/else`、`switch/case/default`、三目、guard、循环内条件——每个分支一条用例
2. **嵌套不笛卡尔积**：内层分支的前置条件写入外层取值；每个判定点自身覆盖真假分支
3. **复合条件拆原子**：`a && b` → 成功 1 条 + 每个原子不满足各 1 条（高安全场景可 MC/DC）
4. 每类结构的**展开细则**（边界值、状态机、循环、try/catch、判空、多 case 等）→ **对照 `references/code-snippet-templates.md`，本文件是"怎么展开"的唯一权威**，生成时先定位场景类型再套用
5. **输入约束分析**：读取参数的框架约束注解与类型（`@NotNull/@Min/@Max/@Pattern/@Size` 等，各语言对照 `references/input-constraints.md`），为每个约束生成等价类用例（合法/非法/边界/空），**独立于分支覆盖**——测"代码隐含的输入约束"。约束用例标注"输入约束"，优先级：必填/格式 P0，范围/长度 P1；框架层拦截的约束预期结果标"框架校验失败"
6. **外部契约建模**：识别代码调用的外部依赖点（`xxxService/xxxDao/xxxClient` 等方法调用，对照 `references/external-contracts.md`），为每个调用点生成**契约五态用例**（成功有效/成功空值/抛异常/超时/降级），把"推断（需联调）"升级为确定性建模——前置条件 mock 依赖到目标契约状态，验证代码对应分支
7. **高级维度分析**：按需启用 `references/advanced-dimensions.md` 的高级维度——**数据流/null 传播、时间/时序/幂等、权限矩阵、容量/计算边界**（常规默认启用）；**MC/DC 与业务链路**（高安全/全链路场景按需启用）。每个维度按其特征识别并生成对应用例，标注对应维度名
8. **用例↔功能可验证（研发视角）**：每条用例必须能回答"它验证哪个业务场景功能的哪个行为"——用例名称用"功能名-触发条件"表达；**备注以 `验证：<功能>-<行为>` 标注验证点**（如"验证：创建订单-用户为空时拒绝并提示"）；覆盖说明提供「功能↔用例」清单（每个功能列出其全部用例编号），供研发核对每个功能的用例覆盖

每条用例填 11 列（`cases.json` 键名一致）：

| 字段 | 填写规则 |
|---|---|
| 用例编号 | `TC-<场景缩写>-<序号>` |
| 业务场景 / 功能 | 第 2 步识别结果 |
| 用例名称 | 统一"功能名-触发条件"，用**业务语义**（如"查询商品-商品不存在"），**不暴露 mapper/dao 等实现名** |
| 前置条件 | **可构造的数据/状态前提**（嵌套写外层取值），给具体值（如"存在 id=1 的商品"），**不与操作步骤混淆** |
| 操作步骤 | **可复现**：给具体调用入口 + 参数值 + mock 方式（如"POST /save，body 传 {id:null,name:'x'}"、"mock mapper.save 抛 SQLException 后调用"） |
| 优先级 | P0=核心/权限/安全校验；P1=主要分支；P2=边缘/兜底/边界 |
| 预期结果 | **核心，必须可判定**——精确到返回体/状态码/文案/异常，或给判定标准（如"返回影响行数>0"、"HTTP 200 + data 含商品对象"）。不可判定的"返回商品数据"类描述不合格；错误串原样摘录；备注标注来源（代码摘录 / 推断需联调） |
| 测试结果 | 留空，下拉 PASS/FAIL |
| 备注 | 聚焦执行者需要的信息：外部依赖、风险、需联调项；**去掉与代码位置列重复的行号引用** |
| 代码位置 | 精确到**判定点/分支所在行** + 方法名（如 `CommondityServiceAbsImpl.java:24 (update)`） |

**字段质量自检（每条用例生成后必须通过）**：
1. 预期结果可判定——执行后能据此判断 PASS/FAIL
2. 前置条件可构造——给出具体数据/状态，而非空泛的"合法/有效"
3. 操作步骤可复现——具体参数/mock 方式，执行者能照做
4. 用例名称用业务语义——不暴露 mapper/dao 等实现名

### 第 4 步 覆盖率核对（机器基准 + 功能级 100% 承诺）
1. 运行 `python scripts/enumerate_decision_points.py <目录> > dp.json`，判定点总数 = 覆盖率**分母**（脚本对某语言误判时人工修正并说明）
2. 判定点清单与用例双向比对：dp.json 有、用例无 → 补；用例无依据 → 删/标
3. **功能级覆盖（研发视角硬承诺）**：
   - **每个功能必须有 ≥1 条用例**；逐功能统计 入口覆盖 + 判定点覆盖 X/Y
   - 纯转发/薄层功能（如 controller 无判定点）归入其委托的功能，不产生独立"空覆盖"功能
   - 任何功能 0 用例必须显式说明原因（无业务逻辑 / 被边界排除 / 并入他处）
   - 覆盖统计同时给 **整体 X/Y** 与 **逐功能清单**（写入 `coverage.per_function`）
4. 无法静态验证项写入 `coverage.notes`

### 第 5 步 规则缺口审查（验证"应做什么"）
代码驱动发现不了"该有却没有"的分支。对照 `references/rule-gap-checklist.md`（金额/库存/权限/防重/并发/状态/必填/时效等）逐条反向审查：
- 规则在代码有对应判定点 → 已覆盖
- **无对应分支 → 写入 `coverage.rule_gaps`**（类别/功能/缺口/建议确认人）；若提供了需求/契约，缺口即疑似缺陷
- 规则缺口不参与判定点覆盖统计，单独列出（避免"100% 覆盖却缺关键规则"的错觉）

### 第 6 步 生成 Excel
整理 `cases.json`（cases + coverage）后运行：
`python scripts/generate_excel.py cases.json --project-name "<项目名>" [--version 1.0]`

- 输出到 skill 安装目录 `testcase/<项目名>/<项目名>_<日期>_<版本>.xlsx`
- 主 sheet 11 列（含 PASS/FAIL 下拉）+「覆盖说明」sheet（统计/追溯矩阵/缺口/标注/规则缺口）
- 用户明确指定路径时遵循用户路径（测试/CI 场景）

### 第 7 步 汇报
- 统计（场景/功能/用例数）+ **功能级覆盖报告**（逐功能 判定点 X/Y，标注是否 100%）+ 整体覆盖率 + 规则缺口 + 输出文件路径 + 风险提示
- **自测执行建议**：给研发"先跑 P0"清单（核心流程/权限/金额用例优先），以及"无法静态验证需联调"的用例

## 输出示例与参考模板

分场景的"代码片段 → 用例模板"见 **`references/code-snippet-templates.md`**（13 类条件结构 + 综合案例 + 资深审查要点）。

简要示例（Java createOrder → 用例节选）：

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 代码位置 |
|---|---|---|---|---|---|---|---|
| TC-ORD-001 | 创建订单 | 用户为空校验 | 调用创建订单接口 | body 传 {userId:null} | P0 | 返回错误"用户不能为空" | OrderService.java:3 |
| TC-ORD-002 | 创建订单 | 库存不足校验 | userId 有效 | body 传 {stock:false} | P0 | 返回错误"库存不足" | OrderService.java:5 |
| TC-ORD-003 | 创建订单 | 微信支付 | 参数有效 | 传 payType=1 | P1 | 支付方式置为 wechat | OrderService.java:8 |
| TC-ORD-004 | 创建订单 | 支付方式不支持 | 参数有效 | 传 payType=3 | P2 | 返回错误"支付方式不支持" | OrderService.java:11 |

## 注意

- 单个函数条件较多时按执行顺序编号，保证可追溯
- 语言差异只影响入口识别与语法写法（`references/language-guide.md`），用例生成逻辑统一
- 所有 rules 的展开、边界、状态机等细节，以 `references/code-snippet-templates.md` 与 `references/rule-gap-checklist.md` 为准
- **执行边界**：扫描/识别/展开时主动收敛（排除生成代码、样板类、非代码文件；限制调用链/嵌套/原子数/用例数上限），详见 `references/execution-boundaries.md`，避免过度执行

---
name: code-scenario-testcases
description: 面向验证设计生成研发自测用例 Excel 表格：把业务代码梳理成 业务场景 → 功能 → 验证点 → 用例 四级结构，每条用例含验证什么（验证点）、怎么验证（验证方法）、能否自动化（可自动化程度）与前置条件/操作步骤/预期结果，并附覆盖率与规则缺口说明。当用户要求"根据代码生成测试用例/自测用例/验证设计/用例清单"、"梳理业务场景和功能点并设计验证方案"、"把某段代码的 if/else/switch 分支整理成验证用例"、"对代码做分支覆盖式自测"、或给出一个代码目录/文件并想要用例表时，使用本 skill。支持命令式指令 `code-scenario-testcases -all`（全量扫描）、`-diff`（当前未提交变更）、`-diff [commitId]`（指定提交之后变更）、`-doc`（仅生成当前项目文档，不生成用例）。语言无关，支持 Java/Go/Python/JS/TS/C#/C++/Rust。
---

# 面向验证设计的研发自测用例生成

## 定位与角色

读代码 + （可选）需求 → 设计 **业务场景 → 功能 → 验证点 → 用例** 四级结构的验证设计文档，输出 Excel。本 skill 产出的是**验证的设计**（测试设计文档）：每条用例回答三个问题——**验证什么**（验证点）、**怎么验证**（验证方法）、**能否自动化**（可自动化程度），而非仅罗列代码分支。

以**资深测试设计架构师**身份执行，自检准则：每条用例可执行（前置可构造、步骤可复现、预期可判定）、验证方法可观察（怎么验证 == 能看到什么）、高风险优先（金额/权限/状态迁移 → P0）、诚实交付（不粉饰边界）、报告专业（统计+覆盖率+缺口+验证计划）。

以**研发自测者**视角执行：你测的是"需求说的行为"，代码只是触发路径——只顺着代码走分支是"代码审计"，不是自测；每条用例必须能回答三个问题：**验证什么**（验证点）、**怎么验证**（验证方法）、**能否自动化**（可自动化程度）。

## 核心原则

1. **验证设计 = 需求验证点 ∪ 代码可测点**：代码判定点是"可测点"的主来源；需求/契约定义"该验证什么"，规则缺口审查负责补齐"代码已写但没有的需求验证点"与"该有却没有的"缺口。只测代码已有分支是"代码审计"，不是"验证设计"
2. **忠实代码**：可测点来自真实代码，不脑补；错误串原样摘录进预期结果
3. **机器基准**：判定点覆盖分母 = `scripts/enumerate_decision_points.py` 枚举的真实判定点，漏测亮红灯；需求验证点覆盖为第二维度，对照需求逐条
4. **不硬凑**：需求未实现/外部依赖/框架隐含三类无法从静态代码对应 → 显式标注（需求未实现场景 / 推断需联调 / 无法静态验证）

## 输入

- 目录（递归扫描）、单个文件/函数、或粘贴的代码片段
- **推荐：需求/契约注入**——附需求描述、接口契约、验收标准。预期结果优先对照需求，生成"需求验证点覆盖 X/Y"，并触发第 6 步规则缺口审查（该有却没有的规则）
- 未指定范围时默认全目录扫描

## 调用方式

| 指令 | 说明 |
|---|---|
| `code-scenario-testcases -all` | 全量扫描整个目录（默认） |
| `code-scenario-testcases -diff` | 只分析未提交变更（`git diff HEAD` + 未跟踪新文件） |
| `code-scenario-testcases -diff [commitId]` | 分析该提交之后到当前（`git diff commitId`） |
| `code-scenario-testcases -doc` | **仅生成当前项目文档**：只执行第 0.5 步项目了解与文档产出（6 份版本化 doc），不生成测试用例 Excel |
| `code-scenario-testcases -help` | 输出基本使用帮助并结束（不执行扫描） |

- **检测到 `-help` 或用户询问"怎么用/有哪些指令"时**：直接输出 `references/help.md` 的内容并结束，不执行扫描流程
- **检测到 `-doc` 时**：只执行第 0.5 步（项目了解与文档产出），产出 `testcase/<项目名>/doc/` 下 6 份版本化文档后结束，不进入第 1~8 步；可附带需求/契约作为文档信息源
- 非 git 仓库或无变更 → 回退全量扫描并说明
- 定位**受影响函数**（变更行所在函数体），以整函数为单位生成完整验证用例（防回归）；`-diff` 额外 grep 变更函数调用方标记"间接受影响"

## 工作流程

### 流程总览（先建立全局心智模型再动手）

| 阶段 | 步骤 | 做什么 | 产出 |
|---|---|---|---|
| 阶段0 前置界定 | 0 需求理解与范围界定 | 成功判据三问 + 改动范围/测试边界界定 | 自测范围说明（并入双基线） |
| 阶段0A 项目了解 | 0.5 全面了解项目 + 产出版本化文档 | 了解背景/需求/架构/流程/功能/场景 → 6 份 doc（`testcase/<项目名>/doc/`） | 版本化项目文档（供第 1/5/6 步复用） |
| 阶段1 验证设计 | 1 扫描过滤 + 建立双基线 | 扫描源码；建入口清单（代码基线）+ 业务规则清单（需求基线） | 场景 → 功能骨架 + 双基线清单 |
| 阶段1 验证设计 | 2 识别场景、功能与验证点 | 代码可测点 ∪ 需求验证点 → 每功能验证点清单 | 功能入口清单 + 验证点清单（覆盖分母） |
| 阶段1 验证设计 | 3 设计验证用例 | 逐验证点可执行化（前置+步骤+预期），套 13 类展开模板 | 15 列用例 |
| 阶段1 验证设计 | 4 高级维度补齐 | 数据流/null、时序/幂等、权限矩阵、容量/边界（A-D 默认；MC/DC、链路按需） | 维度用例（标注维度名） |
| 阶段1 验证设计 | 5 覆盖核对 | 判定点分母比对 + 需求验证点 X/Y + 逐功能 100% 承诺 | 覆盖统计 + 缺口 + 追溯矩阵 |
| 阶段1 验证设计 | 6 规则缺口审查 | 对照业务规则基线逐条反向核对（该有却没有） | rule_gaps |
| 阶段2 执行收尾 | 7 生成 Excel 与汇报 | 字段自检门禁 → 生成 xlsx → 汇报（含自测结论） | .xlsx + cases.json |
| 阶段2 执行收尾 | 8 自测执行与收尾（可选） | 冒烟先行 → FAIL 闭环 → 需求偏差确认 → 回补用例 → 结论 | PASS/FAIL 记录 + 自测结论 |

### 第 0 步 需求理解与范围界定
动笔前先明确"测什么、测到什么程度算过"。回答三个问题（**成功判据三问**）：

1. **给谁用**：本功能的服务角色/入口（哪个页面、哪个接口、谁调用）
2. **输入输出契约**：入参字段、返回结构、状态码（从需求文档/接口契约摘录）
3. **成功判据**：验收标准里可判定的那句话（"订单生成且库存 -1"而非"下单成功"）

再界定本次**测试边界**：
- **改动范围**：本次变更涉及的功能入口（`-diff` 定位受影响函数 + grep 调用方标"间接受影响"）；只测改动波及范围，不无限回溯
- **本功能职责 vs 外部依赖**：外部依赖（DB/缓存/第三方接口/SDK）用 mock 构造，不真测；自测只保证"你的代码在依赖给定返回时行为正确"

产出：**自测范围说明**，并入第 1 步双基线作为"测什么"的起点。

### 第 0.5 步 项目了解与文档产出（阶段0A）
生成用例前**全面了解当前项目**并产出**版本化 doc 文档**，作为后续验证设计的项目背景依据（`-all` 与 `-diff` 均先执行本步；**`-doc` 指令只执行本步即结束**，不生成用例 Excel）。

**了解范围**：项目背景、需求、架构设计、业务流程、功能、使用场景。

**产出 6 份独立 Markdown 文档**，输出到 `testcase/<项目名>/doc/`（按项目名分类，文件名带版本号）：

| 文档 | 内容要点 |
|---|---|
| `项目背景_vN.md` | 定位/解决什么问题/服务对象/技术栈/模块概览 |
| `需求分析_vN.md` | 业务规则（必填/金额/权限/防重/状态机）+ 功能需求 + 非功能需求 |
| `架构设计_vN.md` | 分层/模块/依赖/入口出口/数据流/关键设计 |
| `业务流程_vN.md` | 核心业务主流程 + 分支/异常流程 |
| `功能清单_vN.md` | 场景 → 功能 → 入口方法（供第 5 步覆盖对照） |
| `使用场景_vN.md` | 用户角色 + 典型使用场景 |

每份文档统一头部：`# 标题`、`版本: v<N>`、`生成时间`、`信息来源: 需求注入/代码推断/待确认`、`项目`。**详细模板见 `references/project-docs.md`**。

**版本规则**：
- 命名 `<主题>_v<N>.md`；扫描 `doc/` 下已有 `<主题>_v*.md` 取最高版本
- **信息有实质变化 → 版本 +1**（旧版本保留可回溯）；**无变化 → 复用当前版本**（仅刷新生成时间）
- `-diff` 必有变更 → 递增；`-all` 对比 `git log` 最近提交 → 有变递增、无变复用

**信息源**：需求注入优先；缺的从代码推断并标注 `推断（待确认）`，不假装确定。

**衔接**：功能清单/入口供第 1 步「入口清单（代码基线）」复用；业务规则供第 1/6 步需求基线/规则缺口复用；功能清单供第 5 步功能级覆盖对照。

### 第 1 步 扫描过滤与建立双基线
递归收集源码，按扩展名识别语言（见 `references/language-guide.md`）；**跳过构建产物/依赖目录/测试文件**。

- **依赖库与 SDK 不生成用例**：第三方依赖（jar / node_modules / vendor 等）的内部实现不是被测对象，一律排除；只测**项目自身的业务代码**
- **对依赖的调用判定点仍测**：项目代码里 `if (!stock.check(...))` 这类"调用依赖后的分支"属于业务逻辑，会生成用例——测的是"你的代码如何处理依赖返回"，不是"依赖内部实现"；这类用例预期结果标注 `推断（需联调）` 或 `无法静态验证`

扫描后为本次自测建立**双基线**（"测什么"的依据，也是第 5 步覆盖核对与第 6 步规则缺口审查的对照清单）：
- **入口清单（代码基线）**：识别功能入口（识别规则见第 2 步），产出"场景 → 功能"骨架
- **业务规则清单（需求基线）**：需求/契约/验收标准中的业务规则（必填/金额/权限/防重/时效/状态机/安全）；**无需求文档时**从业务常识与代码可推断规则建立（如扣库存应校验、下单应防重），逐条标注"待与需求确认"

### 第 2 步 识别场景、功能与验证点
入口方法 = 功能（HTTP 路由/注解/导出函数）；相关功能聚为业务场景（同 Controller/同实体/同模块）。

**为每个功能建立「验证点清单」**（验证设计的核心产出，见 `references/verification-points.md`）——列出该功能"值得验证的行为点"，来源分两类：
- **代码可测点**：判定点分支、输入约束、外部契约、状态迁移、异常路径（来自第 3 步各识别规则）
- **需求验证点**：第 1 步业务规则基线逐条对照代码——规则在代码有对应判定点 → 已覆盖；无对应分支 → 待第 6 步列为规则缺口（疑似缺陷）

产出**功能入口清单** + **验证点清单**（作为覆盖分母与追溯依据）。

### 第 3 步 设计验证用例
对每个功能：**先对照第 2 步验证点清单逐点设计用例**（保持映射意识：功能 = 代码入口，验证点 = 一个行为点，用例 = 该验证点的可执行化，预期结果 = 该路径"看到"的现象；对不上就重写或标注）：

1. **判定点 = 可测点**：`if/else`、`switch/case/default`、三目、guard、循环内条件——每个分支一条用例
2. **嵌套不笛卡尔积**：内层分支的前置条件写入外层取值；每个判定点自身覆盖真假分支
3. **复合条件拆原子**：`a && b` → 成功 1 条 + 每个原子不满足各 1 条（高安全场景可 MC/DC）
4. 每类结构的**展开细则**（边界值、状态机、循环、try/catch、判空、多 case 等）→ **对照 `references/code-snippet-templates.md`，本文件是"怎么展开"的唯一权威**，生成时先定位场景类型再套用
5. **输入约束验证点**：读取参数的框架约束注解与类型（`@NotNull/@Min/@Max/@Pattern/@Size` 等，各语言对照 `references/input-constraints.md`），为每个约束生成等价类用例（合法/非法/边界/空），**独立于分支覆盖**——测"代码隐含的输入约束"。约束用例标注"输入约束"，优先级：必填/格式 P0，范围/长度 P1；框架层拦截的约束预期结果标"框架校验失败"
6. **外部契约验证点**：识别代码调用的外部依赖点（`xxxService/xxxDao/xxxClient` 等方法调用，对照 `references/external-contracts.md`），为每个调用点生成**契约五态用例**（成功有效/成功空值/抛异常/超时/降级），把"推断（需联调）"升级为确定性建模——前置条件 mock 依赖到目标契约状态，验证代码对应分支
7. **验证点↔功能可追溯（研发视角）**：每条用例必须能回答"它验证哪个场景功能的哪个验证点"——用例名称用"功能名-触发条件"表达；**备注以 `验证：<功能>-<行为>` 标注验证点**（如"验证：创建订单-用户为空时拒绝并提示"）；覆盖说明提供「功能↔用例」清单（每个功能列出其全部用例编号），供研发逐功能核对
8. **验证方法标注（怎么验证）**：每条用例标注执行者如何观察到结果——枚举定义见 15 列字段表"验证方法"行（`界面操作 / 接口响应 / 数据状态 / 日志 / 需联调`）。**验证方法与预期结果的可观察对象必须匹配**
9. **可自动化程度标注（能否自动化）**：每条用例标注验证策略分层——枚举定义见 15 列字段表"可自动化程度"行（`可单测 / 可组件测试 / 可端到端 / 仅人工+联调 / 需联调确认`）。不确定时标`需联调确认`，不臆测

每条用例填 15 列（`cases.json` 键名与 `scripts/generate_excel.py` 的 `COLUMNS` 一致：`case_id / scenario / function / name / scenario_type / verify_point / precondition / steps / verify_method / auto_level / priority / expected_result / test_result / remark / code_location`）：

| 字段 | 填写规则 |
|---|---|
| 用例编号 | `TC-<场景缩写>-<序号>` |
| 业务场景 / 功能 | 第 2 步识别结果（两列分开） |
| 用例名称 | 统一"功能名-触发条件"，用**业务语义**（如"查询商品-商品不存在"），**不暴露 mapper/dao 等实现名** |
| 场景类型 | **正常流程** / **异常流程**：按该路径**最终现象**分类——正常流程=成功完成功能（校验通过/成功返回/状态变更成功/正常展示）；异常流程=被拒绝或失败（抛异常/错误码/状态拒绝/外部依赖失败）。**看现象不看分支方向**（`if (stockOk){保存}` 真分支=正常；`if (coupon==null) throw` 真分支=异常）。复合条件"成功1条"=正常、"原子不满足"=异常；switch 各 case=正常、default=异常；外部契约成功态=正常、异常/超时/降级=异常。**每个功能至少 1 条正常流程用例（成功主路径）**。**生成 Excel 时「场景类型」列文字着色：正常流程绿色、异常流程红色**（见 `scripts/generate_excel.py`），其余列文字保持默认黑色，目视即可区分成功/拒绝路径 |
| 验证点 | 该用例验证的行为点（判定点分支/输入约束/外部契约/状态迁移/异常路径/业务规则），一句话 |
| 前置条件 | **可构造的数据/状态前提**（嵌套写外层取值），给具体值（如"存在 id=1 的商品"），**不得以"调用/传"开头**（那是步骤），**不与操作步骤混淆**；**开头必须标注触发方式**：`[页面触发]`（步骤为页面 UI 操作，前置需真实页面环境）或 `[mock数据]`（步骤为接口调用/mock 依赖/数据预置，前置只靠数据构造） |
| 操作步骤 | **可复现**：给具体调用入口 + 参数值 + mock 方式（如"POST /save，body 传 {id:null,name:'x'}"、"mock mapper.save 抛 SQLException 后调用"） |
| 验证方法 | **权威定义**：`界面操作`（App 上点选、看弹窗/页面状态）/ `接口响应`（看返回体/状态码）/ `数据状态`（查库/缓存/SharedPreferences）/ `日志`（看 Logcat）/ `需联调`（依赖外部无法本地判定）——执行者如何观察到结果 |
| 可自动化程度 | **权威定义**：`可单测`（纯逻辑分支，JVM+mock 可自动跑）/ `可组件测试`（需 Android/Robolectric 环境）/ `可端到端`（跨界面/跨模块 UI 流程）/ `仅人工+联调`（外部契约/真机环境/需服务端配合）/ `需联调确认`（不确定不臆测） |
| 优先级 | P0=核心/权限/安全校验；P1=主要分支；P2=边缘/兜底/边界 |
| 预期结果 | **核心，必须可判定且单一**——统一为 **`UI显示: <页面/客户端现象>;数据结果: <接口/数据/状态变化>`** 双部分：UI 显示写页面/客户端实际看到的（提示文案/弹窗/页面状态/展示值），数据结果写接口返回体/状态码/数据库/缓存变化（精确到返回体/状态码/文案/异常，或判定标准如"返回影响行数>0"）；纯接口用例 UI 显示写 `无(纯接口)`，纯展示用例数据结果写 `无(纯展示)`；不可判定的"返回商品数据"类描述不合格；错误串原样摘录；备注标注来源（代码摘录 / 推断需联调） |
| 测试结果 | 留空，下拉 PASS/FAIL |
| 备注 | 聚焦执行者需要的信息：外部依赖、风险、需联调项、来源（代码摘录/推断）；**去掉与代码位置列重复的行号引用** |
| 代码位置 | 精确到**判定点/分支所在行** + 方法名（如 `CommondityServiceAbsImpl.java:24 (update)`） |

**字段质量自检（每条用例生成后必须通过）**：
1. 预期结果可判定——执行后能据此判断 PASS/FAIL
2. **预期单一可判定**——预期结果不得含"或/可能"两个结果；确有不确时写"预期 X；若出现 Y 视为缺陷"
3. **可观察对象 = 研发实际看到的**——写接口响应/页面/日志等外部可观察物，不写 mapper 返回值等内部细节；且必须与「验证方法」列匹配（验证方法说"看日志"，预期就必须给出日志里的判定点）
4. **依赖未知实现先确认**——涉及全局异常/框架行为的预期，标注"需联调确认具体响应"，不臆测状态码/结构
5. 前置条件可构造——给出具体数据/状态，而非空泛的"合法/有效"
6. **前置条件不写成操作**——不得以"调用/传"开头（操作应写入步骤列）
7. 操作步骤可复现——具体参数/mock 方式，执行者能照做；外部依赖 mock 写明注入方式
8. 用例名称用业务语义——不暴露 mapper/dao 等实现名
9. 代码位置精确——到判定点/分支所在行 + 方法名
10. **场景类型标注正确**——正常流程用例预期为成功完成（返回成功/状态变更成功），异常流程用例预期为拒绝/失败（抛异常/错误码），两者不得互换
11. **前置条件触发方式标注正确**——前缀 `[页面触发]`/`[mock数据]` 与操作步骤入口方式一致（页面操作→页面触发；接口调用/mock/数据预置→mock数据），不混用不臆测
12. **预期结果双结果完整**——含 `UI显示:` 与 `数据结果:` 两部分；纯接口写 `UI显示: 无(纯接口)`、纯展示写 `数据结果: 无(纯展示)`，不省略任何一部分

### 第 4 步 高级维度补齐
基础分支之外的**高危/复杂行为**按维度识别补充用例（详见 `references/advanced-dimensions.md`），默认启用 A-D，E/F 按需启用：

| 维度 | 识别特征 | 用例 |
|---|---|---|
| A 数据流 / null 传播 | 方法返回对象被立即判空（`dao.findById` → `if (order == null)`） | 该调用返回 null → 验证下游所有判空分支 |
| B 时间 / 时序 / 幂等 | 时间比较、写操作、关键链路 | 过去/现在/未来三态；重复提交拦截/幂等；乱序操作 |
| C 权限矩阵 | role/hasRole/ownerId 字段 | 角色 × 操作 × 资源归属矩阵（本人/他人/管理员 × 允许/拒绝） |
| D 容量 / 计算边界 | limit、批量、集合上限、浮点金额、除零 | 上限 / 边界±1 / 溢出用例 |
| E MC/DC（按需） | 金额/权限/支付中的复合条件 | 每个原子条件独立翻转判定 |
| F 业务链路（按需） | 跨函数/跨服务关键链 | 每步正常 + 每步失败中断（单链 ≤10 条） |

每条维度用例在备注标注对应维度名，供第 5 步核对。

### 第 5 步 覆盖核对（机器基准 + 需求验证点 + 功能级 100% 承诺）
1. 运行 `python scripts/enumerate_decision_points.py <目录> > dp.json`，判定点总数 = 判定点覆盖**分母**（脚本对某语言误判时人工修正并说明）
2. 判定点清单与用例双向比对：dp.json 有、用例无 → 补；用例无依据 → 删/标
3. **需求验证点覆盖（若注入需求/契约）**：第 2 步验证点清单中的"需求验证点"逐条对照用例，给出 **X/Y**；无用例对应的需求验证点写入 `coverage.rule_gaps` 或 `coverage.verification_gaps`
4. **高级维度检查点**：A-D 高危点（null 传播、幂等、权限、容量边界）逐条确认已有维度用例，无 → 补
5. **功能级覆盖（研发视角硬承诺）**：
   - **每个功能必须有 ≥1 条用例**；逐功能统计 入口覆盖 + 验证点覆盖 + 判定点覆盖 X/Y
   - 纯转发/薄层功能（如 controller 无判定点）归入其委托的功能，不产生独立"空覆盖"功能
   - 任何功能 0 用例必须显式说明原因（无业务逻辑 / 被边界排除 / 并入他处）
   - 覆盖统计同时给 **整体 X/Y** 与 **逐功能清单**（写入 `coverage.per_function`）
6. 无法静态验证项写入 `coverage.notes`；可自动化程度分布写入 `coverage.auto_level_distribution`（供验证计划编排）

### 第 6 步 规则缺口审查（需求验证点缺代码支撑 → 待验证/疑似缺陷）
代码驱动发现不了"该有却没有"的分支。**对照第 1 步业务规则基线**逐条反向核对（规则类别对照 `references/rule-gap-checklist.md`：金额/库存/权限/防重/并发/状态/必填/时效等）：
- 规则在代码有对应判定点 → 已覆盖（成为"需求验证点"并已有用例）
- **无对应分支 → 写入 `coverage.rule_gaps`**（类别/功能/缺口/建议确认人）；若提供了需求/契约，缺口即疑似缺陷，并作为"待补需求验证点"
- 规则缺口不参与判定点覆盖统计，单独列出（避免"100% 覆盖却缺关键规则"的错觉）

### 第 7 步 生成 Excel 与汇报
**先过发布门禁**：第 3 步的 12 条字段质量自检逐条复查（尤其预期可判定、前置带触发方式前缀、预期 UI显示/数据结果 双部分完整、可观察对象与验证方法匹配），不合格的用例退回第 3 步重写。

整理 `cases.json`（cases + coverage）后运行：
`python scripts/generate_excel.py cases.json --project-name "<项目名>" [--version 1.0]`

- 输出到 skill 安装目录 `testcase/<项目名>/<项目名>_<日期>_<版本>.xlsx`
- 前置文档：`testcase/<项目名>/doc/` 下 6 份版本化项目文档（第 0.5 步产出）
- 主 sheet 15 列（含 场景类型/测试结果 下拉）+「覆盖说明」sheet（统计/追溯矩阵/验证点覆盖/可自动化分布/场景类型分布/缺口/标注/规则缺口）
- **阅读体验**：主 sheet 行高按内容自适应估算（换行内容完全显示）、数据行斑马纹、统一字体、场景类型列着色（正常绿/异常红）；覆盖说明分节标题浅蓝、子表蓝底白字表头、全表行高自适应
- 用户明确指定路径时遵循用户路径（测试/CI 场景）

汇报：
- 统计（场景/功能/验证点/用例数）+ **功能级覆盖报告**（逐功能 判定点 X/Y + 需求验证点 X/Y，标注是否 100%）+ 整体覆盖率 + 规则缺口 + 输出文件路径 + 风险提示
- **验证计划**（替代"自测执行建议"）——按可自动化程度分层给出验证策略：
  - **冒烟/优先（先跑 P0）**：核心流程、权限、金额用例清单（对应"可单测/可端到端"中的 P0）
  - **回归层**：标注"可单测"的用例 → 建议转自动化防回归
  - **深度层**：边界/兜底/并发等 P1/P2 用例
  - **联调层**：标注"仅人工+联调/需联调确认"的用例（外部契约、跨系统）
- **自测结论**：`全部通过 / 阻塞项已修复 / 存在遗留缺陷 N 项（附清单）`，并明确**是否可提测**
- 风险提示补充**需求偏差项**（代码与需求不一致、待与产品/负责人确认的事项）

### 第 8 步 自测执行与收尾（可选——仅当本次需要执行闭环时启用；纯设计产出场景可跳过）
设计产出交付后，研发按下列顺序执行用例并收尾：

1. **冒烟先行**：先跑 P0 主流程用例，主流程断了先修，不浪费时间测分支
2. **FAIL 闭环**：`用例 FAIL → 定位原因 → 修复代码 → 重跑该条 → 重跑相邻用例`（防修复引入新问题）
3. **需求偏差确认**：代码和需求对不上时**停下确认**（产品/负责人），不擅自按自己理解测
4. **回补用例**：把本次发现的有价值场景回补进用例集，作为下次回归基线
5. **收尾交付**：汇总统计（用例总数 / PASS / FAIL / 覆盖率）+ 遗留问题清单 + **自测结论**（是否可提测）

## 附录：人工自测 8 列模板（快速场景）

**触发条件**：你只要快速人工自测、不需要 Excel 时，直接输出下列模板；需要完整 Excel 时走第 0~8 步流程。

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 验证方法 | 优先级 | 结果 |
|---|---|---|---|---|---|---|---|
| TC-ORD-001 | 下单-正常流程 | [mock数据] 登录态有效，库存=5 | 提交订单，数量=1 | UI显示: 无(纯接口);数据结果: HTTP 200，返回订单号，库存变 4 | 接口响应+查库 | P0 | PASS |
| TC-ORD-002 | 下单-库存不足 | [mock数据] 登录态有效，库存=1 | 提交订单，数量=2 | UI显示: 无(纯接口);数据结果: 返回"库存不足"，订单不生成 | 接口响应 | P0 | FAIL |
| ... | | | | | | | |

**字段填写纪律**（与第 3 步一致）：用例名称用"功能名-触发条件"业务语义；前置条件给具体数据、不以"调用/传"开头；操作步骤可复现（入口+参数）；预期结果统一为 `UI显示:...;数据结果:...` 双部分且可判定（精确到文案/状态码）；前置条件带 `[页面触发]`/`[mock数据]` 前缀；验证方法为 `界面操作 / 接口响应 / 数据状态 / 日志 / 需联调`。可自动化程度列省略（快速场景均视为人工执行）；需要完整列时走第 0~8 步流程。

## 输出示例与参考模板

分场景的"代码片段 → 用例模板"见 **`references/code-snippet-templates.md`**（13 类条件结构 + 综合案例 + 资深审查要点）。
验证点概念与识别见 **`references/verification-points.md`**。

简要示例（Java createOrder → 用例节选，含新增列）：

| 用例编号 | 功能 | 用例名称 | 场景类型 | 验证点 | 前置条件 | 操作步骤 | 验证方法 | 可自动化程度 | 优先级 | 预期结果 | 代码位置 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-ORD-001 | 创建订单 | 用户为空校验 | 异常流程 | 必填：userId 非空（业务规则） | [mock数据] 创建订单接口可用，无历史订单依赖 | POST /order，body 传 {userId:null} | 接口响应 | 可单测 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"用户不能为空"，不生成订单 | OrderService.java:3 |
| TC-ORD-002 | 创建订单 | 库存不足校验 | 异常流程 | 库存规则（外部契约） | [mock数据] userId 有效；mock 库存校验返回 false | POST /order，body 传 {stock:false} | 接口响应 | 可单测 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"库存不足" | OrderService.java:5 |
| TC-ORD-003 | 创建订单 | 微信支付 | 正常流程 | 支付方式选择（判定点） | [mock数据] userId 有效，库存充足 | POST /order，传 payType=1 | 数据状态 | 可单测 | P1 | UI显示: 无(纯接口);数据结果: 订单 payChannel 置为 wechat（查库确认） | OrderService.java:8 |
| TC-ORD-004 | 创建订单 | 支付方式不支持 | 异常流程 | 兜底分支（判定点） | [mock数据] userId 有效 | POST /order，传 payType=3 | 接口响应 | 可单测 | P2 | UI显示: 无(纯接口);数据结果: 返回错误"支付方式不支持" | OrderService.java:11 |

## 注意

- 单个函数验证点较多时按执行顺序编号，保证可追溯
- 语言差异只影响入口识别与语法写法（`references/language-guide.md`），验证设计逻辑统一
- 所有 rules 的展开、边界、状态机等细节，以 `references/code-snippet-templates.md` 与 `references/rule-gap-checklist.md` 为准
- **执行边界**：扫描/识别/展开时主动收敛（排除生成代码、样板类、非代码文件；限制调用链/嵌套/原子数/验证点数/用例数上限），详见 `references/execution-boundaries.md`，避免过度执行

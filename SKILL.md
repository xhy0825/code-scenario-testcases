---
name: code-scenario-testcases
description: 根据业务代码生成研发自测用例 Excel 表格：把业务代码梳理成 业务场景 → 功能 → if/else/switch 条件分支，每条用例含前置条件、操作步骤、预期结果（不通过分支写出功能展示的错误提示）与覆盖率说明。当用户要求"根据代码生成测试用例/自测用例/用例清单"、"梳理业务场景和功能点"、"把某段代码的 if/else/switch 分支整理成测试用例"、"对代码做分支覆盖式自测"、或给出一个代码目录/文件并想要用例表时，使用本 skill。支持命令式指令 `code-scenario-testcases -all`（全量扫描）、`-diff`（当前未提交变更）、`-diff [commitId]`（指定提交之后变更）。语言无关，支持 Java/Go/Python/JS/TS/C#/C++/Rust。
---

# 从代码逻辑生成研发自测用例

## 定位

研发自测是开发在提测前用真实代码逻辑反推出的验证清单。本 skill 不凭空写用例，而是**读代码**：把业务代码中的条件逻辑忠实翻译成 **业务场景 → 功能 → 条件分支用例** 三级结构，输出 Excel 表格。因为用例源自真实分支，能保证"每个判断点都被测到"，避免漏测，也能让研发自测和代码逐行对得上。

## 映射原则（需求/功能/用例 ↔ 代码 场景/条件/现象）

本 skill 从代码**反向推导**业务链，全程遵循四条恒定对应关系：

| 业务侧 | 代码侧 | 本 skill 的落点 |
|---|---|---|
| 需求 | 功能边界 | 需求落到代码 = 一组功能入口；需求没进代码的部分无法覆盖，显式标注 |
| 功能 | 业务入口方法 | 每个功能 = 一个接口/方法入口（第 2 步识别） |
| 测试用例 | 条件分支（路径场景） | 每条用例 = 一条"条件定死后的执行路径"；前置条件 = 条件取值，操作步骤 = 触发路径 |
| 预期结果 | 现象 | 预期结果 = 该路径执行后的**可观察输出**（错误串原样摘录、返回值、状态变化、页面展示） |

生成每条用例时始终自问：**这条用例对应代码里哪条路径？该路径执行后"看到"什么？** 对不上就重写或标注。

## 三级结构

1. **业务场景**：用户视角的一次业务动作（如"创建订单"、"用户登录"），由若干相关功能聚合而成
2. **功能**：一个可独立触发的业务入口（一个接口/路由/方法）
3. **条件分支用例**：该功能代码中每个判定分支对应一条用例

## 输入

- 通常是**目录**：递归扫描其中源码文件
- 也支持单个文件、单个函数、或粘贴的代码片段
- 用户没指定范围时默认扫描全目录，并在汇报中说明覆盖范围

## 调用方式与扫描范围

支持命令式指令控制扫描范围，优先级高于默认全量：

| 指令 | 含义 | 扫描范围 |
|---|---|---|
| `code-scenario-testcases -all` | 全量扫描 | 整个目录的业务代码（默认行为） |
| `code-scenario-testcases -diff` | 当前变更 | 仅未提交变更（工作区+暂存区+未跟踪新文件）涉及的代码 |
| `code-scenario-testcases -diff <commitId>` | 指定提交之后 | 从 `<commitId>` 到当前（含未提交）的变更代码 |

### 用 git 确定变更范围

- `-diff`：`git diff HEAD --name-only`（工作区+暂存区）并合并 `git ls-files --others --exclude-standard`（未跟踪新文件）；跳过被删除文件
- `-diff <commitId>`：`git diff <commitId> --name-only`（该提交之后到当前的全部变更），同样并入未跟踪新文件
- 过滤出源码文件（按扩展名，见 `references/language-guide.md`）；测试文件默认跳过
- 目录不是 git 仓库、或变更集为空时：回退为全量扫描，并在汇报中说明原因

### 定位受影响函数

1. 用 `git diff -U0 -- <文件>` 拿到变更行号
2. 定位变更行所在的**函数体**（含被删改的代码行）
3. 以"受影响函数"为单位生成**完整分支用例**——一次改动可能回归到该函数其它分支，整函数覆盖比"只改哪测哪"更安全
4. 受影响用例的**备注**中标注"本次变更影响"；覆盖说明中单列**变更范围**（N 文件 / M 函数 / 涉及判定点）
5. 覆盖统计以受影响函数的判定点为分母

## 工作流程

### 第 1 步 扫描与过滤代码

- 递归收集源码文件，按扩展名识别语言（对照 `references/language-guide.md`）
- **跳过**：构建产物（target/build/dist/bin/out）、依赖目录（node_modules/vendor/.venv/site-packages）、版本控制目录（.git/.svn）
- **跳过**：测试文件（`_test.go` / `test_*.py` / `*Test.java` / `*.spec.ts` 等）——它们是测试而非被测代码；用户明确要求时才包含
- 目录过大或含多种语言时，优先分析用户指向的模块/子目录，并在汇报中说明

### 第 2 步 识别业务场景与功能

**功能** = 可被外部触发的业务入口，识别启发式（按语言，见 `references/language-guide.md`）：
- HTTP 路由/接口方法：`@GetMapping`、`@app.route`、`r.GET`、`router.post`、`[HttpGet]` 等
- 无框架时：public/导出函数、业务类的方法

**业务场景** = 把相关功能聚合为一组用户动作：
- 聚合依据（优先级从高到低）：① 同一 Controller/类的功能；② 操作同一业务实体的功能（同一资源的增删改查）；③ 同一业务模块（包名/目录）的功能
- 场景命名用用户视角动宾短语：如"创建订单"、"查询订单"、"取消订单" 可归为"订单管理"
- 单一职责的类：一个类 ≈ 一个场景，类内各方法 = 各功能
- 产出**功能入口清单**：每个入口要么"已分析"，要么"显式排除+原因"（如工具函数、非业务入口）。该清单是后续覆盖率的**分母**，供下一步逐功能分析

### 第 3 步 逐功能提取条件分支并生成用例

对清单中每个功能（记住映射原则：功能 = 一个代码入口，其下每条用例 = 一条条件路径，预期结果 = 该路径的现象）：

1. 读函数体，从入口到出口跟踪控制流
2. 识别所有**判定点**（条件逻辑在各语言写法不同，但建模统一，见 `references/language-guide.md`）：
   - `if / else if` → 每个条件拆成【真分支】+【假分支】
   - `else` → 前序判断的兜底假分支
   - `switch(x)` → 每个 `case` + `default`
   - 三目运算符、guard clause（提前 `return`/`throw`）
   - 循环内 `if` + `break`/`continue`
3. 每个判定点的每个分支 = 一条用例
4. **嵌套条件**：内层分支的可达性依赖外层。把外层条件取值写进内层用例的**前置条件**；按"每个判定点自身覆盖真假分支"展开，**不做笛卡尔积**全组合，避免用例爆炸
5. **复合条件拆分**：判定条件是复合布尔表达式（含 `&&` / `||`）时，不满足于整条真/假，而是：
   - **成功路径** 1 条：所有原子条件同时满足
   - **每个原子条件"不满足"各 1 条**：只破坏其中一个原子条件（其余保持满足），让自测者明确判定为假的"成因"
   - 示例：`if (user != null && user.getStatus() == ACTIVE)` → 成功 1 条 + `user=null` 1 条 + `status != ACTIVE` 1 条
   - 原子条件以 `&&` / `||` 为界拆分，不再细分（`a.getX() > 0` 视为一个原子条件）
   - 高安全场景（金额、权限、支付）可进一步按 MC/DC 展开：每个原子条件独立改变判定结果
6. **边界值用例**：对 `>`、`<`、`>=`、`<=`、`==` 等比较判定，在真/假两条主用例外补充边界用例：
   - 等于边界值（`amount == 100`）、边界紧邻两侧（`99` / `101`）
   - `null` / 空集合 / 空串（语义允许时）
   - 极值（MAX/MIN）可选
   - 边界用例在"用例名称"标注"边界"，优先级归 P2，避免与主分支混淆
7. **状态机建模**：识别"状态字段"（形如 `order.getStatus()` / `order.status`，被 `setStatus()`/赋值改写，并与状态常量 `==` 比较）：
   - 列出该状态的全部取值（从枚举/常量推断；无法推断时显式标注）
   - 对"读取状态"的判定点，前置条件覆盖**每个状态取值**（如 PAID / UNPAID / CANCELLED 各一条）
   - 标注状态迁移（谁把状态改成什么），生成"状态到达路径"用例（先到达某状态、再触发该分支）
8. **调用链分析**：场景内从入口函数沿调用向下追踪业务方法：
   - 记录 入口 → 直接调用 → 间接调用 的调用路径
   - 跨函数参数传递 + 状态变化的路径组合要生成用例，每个被调函数的分支用例标注其调用方上下文
   - `-diff` 模式额外用 grep 找变更函数的**直接调用方**，标记"间接受影响"并纳入用例
9. 每条用例填写以下字段：

| 字段 | 填写规则 |
|---|---|
| 用例编号 | `TC-<场景缩写>-<序号>`，按场景内顺序编号 |
| 用例名称 | "功能名 + 分支语义"，如"创建订单-用户为空校验" |
| 前置条件 | 触发该分支所需的数据/状态前提：如"已登录且为管理员"、"订单状态=已支付"、"入参 userId 为 null" |
| 操作步骤 | 可复现步骤：调用入口 + 关键参数 + 数据准备，如"调用 POST /api/order/create，body 传 {userId:null}" |
| 优先级 | P0=核心流程/权限安全校验；P1=主要业务分支；P2=边缘/兜底 |
| 预期结果 | **核心字段**。不通过/校验失败/异常分支 → 写出该功能在界面上**展示的结果**：错误码+错误信息、提示文案、跳转、抛出的异常类型。通过分支 → 写正常返回结果。若代码中直接返回错误串（如 `throw new BizException("库存不足")`、`return errors.New("...")`），**原样摘录**进预期结果。每条预期结果在**备注**标注来源：错误串原样摘录 → `预期结果来源：代码摘录`；正常路径/外部依赖推断 → `预期结果来源：推断（需联调）` |
| 测试结果 | **留空**，供研发自测执行时手动勾选（列内下拉选项 PASS/FAIL），不预填 |
| 备注 | 留空或写补充说明：外部依赖、待验证事项、边界提示等 |
| 代码位置 | 分支所在 `文件:行号` |

### 第 4 步 覆盖率核对（机器基准 + 双向盘点）

覆盖率的**分母用脚本确定性枚举的判定点**，而不是模型自行记忆的判定点——漏了会亮红灯，而不是分母悄悄变小：

1. **机器枚举判定点**：先运行捆绑脚本 `python scripts/enumerate_decision_points.py <扫描目录> > dp.json`，得到按语言语法枚举的**真实判定点清单**（文件/行号/类型/是否复合条件/是否含边界比较）。`dp.json` 的判定点总数 = 覆盖率的**分母**。脚本不可用或对某语言明显误判时，退化为人工盘点并在汇报中说明
2. **判定点盘点清单**：把每个判定点与用例编号对应，格式如下（可与 dp.json 逐条比对）：
   ```
   功能：创建订单   判定点盘点
   L3  if (req.getUserId() == null)            → TC-ORD-001(真) / TC-ORD-002(假)
   L5  if (!req.getStock())                    → TC-ORD-003(真)
   L8  switch (req.getPayType()) 1/2/default   → TC-ORD-004 / 005 / 006
   ```
3. **双向核对**：重新读一遍源码，并与 dp.json 逐条比对：
   - dp.json 有判定点、用例没有 → **补用例**（计入缺口）
   - 有用例但 dp.json 与源码都找不到依据 → 删除或标注
   - **复合条件**判定点（含 `&&`/`||`）核对子条件是否都拆了（第 3 步规则 5）
   - **边界比较**判定点（`>`/`<`/`>=`/`<=`/`==`）核对边界用例（第 3 步规则 6）
4. **入口清单核对**：确认第 2 步清单中每个入口都已"分析"或"显式排除+原因"
5. **覆盖率统计**：
   - 入口覆盖 = 已分析入口数 / 识别入口总数
   - 判定点覆盖 = 已生成用例的判定点数 / **dp.json 判定点总数**（机器基准）
   - 未达 100% 时，明确列出缺口（哪个文件哪一行哪个判定点缺用例）
6. **记录无法静态验证项**：外部依赖行为、需求未实现场景、框架隐含行为——不参与覆盖统计但显式标注
7. 把盘点清单、追溯矩阵、覆盖统计（含机器枚举总数）、缺口、标注整理为 `coverage` 对象，随 `cases.json` 交给下一步

### 第 5 步 生成 Excel

1. 把全部用例整理成结构化 JSON：顶层含 `cases`（每行一条用例，字段名与上方表格的英文键一致）与 `coverage`（第 4 步整理的对象）
2. **输出到 skill 的 testcase 目录**：在 SKILL.md 所在目录（skill 安装目录）维护 `testcase/` 输出区——
   - **多项目按项目名建子文件夹**：`<skill目录>/testcase/<项目名称>/`
   - **文件命名**：`<项目名称>_<日期>_<版本>.xlsx`（如 `订单管理系统_20260801_v1.0.xlsx`）
   - 版本默认 `v1.0`，用户可指定；日期取当天 `YYYYMMDD`
   - 运行捆绑脚本：`python scripts/generate_excel.py cases.json --project-name "<项目名称>" [--version 1.0]`
     - 脚本自动创建 `<skill目录>/testcase/<项目名称>/` 并按上述规则命名
     - 脚本用 openpyxl 生成 `.xlsx`：主 sheet 含表头加粗+底色、自动列宽、冻结首行、带筛选；**测试结果列自带 PASS/FAIL 下拉**；备注列为自由文本
     - 若 JSON 含 `coverage`，脚本额外生成**「覆盖说明」sheet**：覆盖统计、判定点→用例追溯矩阵、缺口列表、无法静态验证项
   - 若任务明确指定了输出路径，遵循指定路径（测试/CI 场景）
3. 环境缺 openpyxl 时，提示用户先执行 `pip install openpyxl`

### 第 6 步 汇报

- 统计：业务场景数、功能数、用例总数，以及各场景/功能分布
- **覆盖率汇报**：入口覆盖 X/Y、判定点覆盖 X/Y；未达 100% 时列出缺口；说明哪些项因代码无法表达而被显式排除
- 给出 Excel 文件路径（`<skill目录>/testcase/<项目名>/<项目名>_<日期>_<版本>.xlsx`，含「覆盖说明」sheet）；用例较多时建议用户按场景/优先级筛选

## 输出示例与参考模板

分场景的**代码片段 → 测试用例模板**已抽离到 `references/code-snippet-templates.md`，覆盖 13 类条件结构（简单 if、if-else、多路链、嵌套、switch、复合条件、三目、循环内条件、边界比较、状态机、try/catch、判空、多 case 合并/类型判断）。生成用例时**先对照该模板定位场景类型**，再按第 3 步规则（嵌套不笛卡尔积、复合拆原子、边界补值）套用展开。

简要示例（Java createOrder 片段 → 用例节选）：

**代码片段：**
```java
public Order createOrder(OrderReq req) {
    if (req.getUserId() == null) {              // L3
        throw new BizException("用户不能为空");
    }
    if (!req.getStock()) {                      // L5
        throw new BizException("库存不足");
    }
    switch (req.getPayType()) {                 // L8
        case 1: order.setPay("wechat"); break;
        default: throw new BizException("支付方式不支持");
    }
    return order;
}
```

**生成的用例（节选）：**

| 用例编号 | 场景 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 测试结果 | 备注 | 代码位置 |
|---|---|---|---|---|---|---|---|---|---|---|
| TC-ORD-001 | 订单管理 | 创建订单 | 创建订单-用户为空校验 | 调用创建订单接口 | body 传 {userId:null} | P0 | 返回错误"用户不能为空"，不生成订单 |  |  | OrderService.java:3 |
| TC-ORD-002 | 订单管理 | 创建订单 | 创建订单-库存不足校验 | userId 有效 | body 传 {stock:false} | P0 | 返回错误"库存不足"，不生成订单 |  |  | OrderService.java:5 |
| TC-ORD-003 | 订单管理 | 创建订单 | 创建订单-微信支付 | 参数有效 | 传 payType=1 | P1 | 订单支付方式置为 wechat，返回订单 |  |  | OrderService.java:8 |
| TC-ORD-004 | 订单管理 | 创建订单 | 创建订单-支付方式不支持 | 参数有效 | 传 payType=3 | P2 | 返回错误"支付方式不支持" |  |  | OrderService.java:11 |

## 注意

- **忠实于代码**：分支来自真实代码，不脑补不存在的条件；代码写死/未实现的行为如实标注
- **语言无关**：条件逻辑建模统一，语言差异只影响入口识别与语法写法（对照 `references/language-guide.md`）
- 单个函数条件较多时，按执行顺序给用例编号，保证可追溯
- **覆盖率口径**：判定点覆盖以"机器枚举的判定点 + 代码可表达范围"为分母（见第 4 步）；外部依赖行为、需求未实现的场景无法由静态代码保证，显式标注而非假装覆盖
- **架构原则（解析器枚举骨架 + LLM 语义化）**：判定点骨架（位置/类型/数量）由 `scripts/enumerate_decision_points.py` 确定性枚举，作为覆盖率基准；LLM 负责语义层（场景命名、前置条件、操作步骤、预期结果）；覆盖报告由机器基准与人工盘点双向交叉得出
- **映射缺口处理**：需求↔功能↔用例↔代码的闭环只在"需求已落到代码"时成立。需求未实现、外部依赖行为、框架隐含行为三类无法从业务代码静态对应——分别标注"需求未实现场景"、"推断（需联调）"、"无法静态验证项"，**不硬凑用例**

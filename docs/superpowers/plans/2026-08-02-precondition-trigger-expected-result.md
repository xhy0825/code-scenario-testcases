# v1.20 前置条件触发方式标注 + 预期结果双结果分离 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 skill 产出的 Excel 用例「前置条件」强制标注 `[页面触发]`/`[mock数据]` 触发方式、「预期结果」强制分离 `UI显示:`（页面/客户端现象）与 `数据结果:`（接口/数据/状态变化）两部分。

**Architecture:** 纯文档改动，列结构不变（15 列）。三处同步——① `references/code-snippet-templates.md` 46 条模板示例 + `input-constraints.md` 5 条 + `external-contracts.md` 4 条全部改新格式；② `SKILL.md`：字段表两行改写、字段质量自检 10→12 条、输出示例表、附录人工自测模板；③ `README.md`：能力表/主表列说明/cases.json 示例/关键设计决策/更新日志。

**Tech Stack:** Markdown 文档 + grep 回归验证。无 Python 改动。

**Spec:** `docs/superpowers/specs/2026-08-02-precondition-trigger-expected-result-design.md`

## Global Constraints

- 只改 `SKILL.md`、`README.md`、`references/code-snippet-templates.md`、`references/input-constraints.md`、`references/external-contracts.md`；`scripts/*.py`、`evals/`、其他 references 一律不动
- **前置条件格式**：`[页面触发] <页面状态前提>` 或 `[mock数据] <数据/状态前提>`；判定看操作步骤入口方式——页面操作（点击/输入/进入页面）→ `[页面触发]`；接口调用/mock 依赖/数据预置 → `[mock数据]`；前置需真实页面环境到达 → `[页面触发]`，只靠数据构造 → `[mock数据]`
- **预期结果格式**：`UI显示: <页面/客户端现象>;数据结果: <接口/数据/状态变化>`；纯接口写 `UI显示: 无(纯接口)`；纯展示写 `数据结果: 无(纯展示)`；两者都有则都写。错误串原样摘录、来源标注（代码摘录/推断需联调）保留在备注
- **模板改写规则**（套用于所有 references 模板示例）：
  - 步骤为"传参/调接口/mock"→ 前置 `[mock数据]`、预期 `UI显示: 无(纯接口);数据结果: <原预期>`
  - 步骤为"进入页面/页面操作"→ 前置 `[页面触发]`、预期 `UI显示: <展示现象>;数据结果: 无(纯展示)`
  - 既有 `预期结果来源`/`状态机`/`边界` 等备注信息**保留原样**，仅改前置与预期两列
- 版本 v1.20；commit 信息格式 `feat:`/`docs:`；每次 commit 触发 post-commit 自动部署属预期行为
- 发布门禁：SKILL.md 字段质量自检须为 12 条（含新增 11/12）

---

### Task 1: references/code-snippet-templates.md 46 条模板示例同步

**Files:**
- Modify: `references/code-snippet-templates.md`（场景 1-13 模板表 + 综合案例表 + 通用用例列说明）

**Interfaces:**
- Produces: 该文件 46 条示例用例行（`^| TC-`）前置条件均含 `[页面触发]`/`[mock数据]`、预期结果均含 `UI显示:`/`数据结果:` 双部分

**转换规则（按表套用）：**
- 场景 7（三目，页面展示）→ 前置 `[页面触发]`、预期 `UI显示: <展示>;数据结果: 无(纯展示)`
- 其余全部场景（接口/数据）→ 前置 `[mock数据]`、预期 `UI显示: 无(纯接口);数据结果: <原预期>`
- 每条前置保留原数据前提，仅加前缀；预期保留原判定内容，仅拆成双部分

- [ ] **Step 1: 记录基线（确认改写前状态）**

Run: `cd /d/AgentDev/code-scenario-testcases && grep -c "UI显示:" references/code-snippet-templates.md; grep -c "^| TC-" references/code-snippet-templates.md`
Expected: `0`（当前无 `UI显示:`）；`46`（示例用例行基线）

- [ ] **Step 2: 改写「通用用例列」说明**

在文件第 5-10 行「通用用例列」表头说明处，`前置条件` 列说明后补触发方式规则、`预期结果` 说明补双结果规则。将第 10 行说明改为：

```
用例编号占位符：`TC-<场景缩写>-<序号>`；业务场景/功能按实际聚合填写；测试结果列留空（下拉 PASS/FAIL）；代码位置填 `文件:行号`。**前置条件**统一以 `[页面触发]`/`[mock数据]` 开头明确触发方式（页面操作→页面触发；接口/mock/数据预置→mock数据）；**预期结果**统一为 `UI显示: <页面/客户端现象>;数据结果: <接口/数据/状态变化>` 双部分（纯接口写 `UI显示: 无(纯接口)`，纯展示写 `数据结果: 无(纯展示)`）。
```

- [ ] **Step 3: 改写场景 1（简单 if 校验）模板表**

将场景 1 用例表两行改为：

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 创建订单 | 用户为空校验 | [mock数据] 调用创建订单接口可用 | body 传 {userId:null} | P0 | UI显示: 无(纯接口);数据结果: 返回错误"用户不能为空"，不生成订单 | 预期结果来源：代码摘录 |
| TC-002 | 创建订单 | 用户非空-正常 | [mock数据] userId 有效 | 正常传参 | P1 | UI显示: 无(纯接口);数据结果: 校验通过，继续执行后续逻辑 | 预期结果来源：推断 |

- [ ] **Step 4: 改写场景 2（if-else 二分支）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 取消订单 | 已支付-取消成功 | [mock数据] 订单状态=PAID | 取消该订单 | P0 | UI显示: 无(纯接口);数据结果: 状态置 CANCELLED，返回订单 | 状态机：PAID→CANCELLED；代码摘录 |
| TC-002 | 取消订单 | 非支付-不可取消 | [mock数据] 订单状态非 PAID | 取消该订单 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"当前订单状态不可取消" | 状态机：非 PAID；代码摘录 |

- [ ] **Step 5: 改写场景 3（if-else if-else 链）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 定级 | 金额>1000 | [mock数据] amount=1001 | 正常下单 | P1 | UI显示: 无(纯接口);数据结果: 等级置 L3 | 边界：1000/1001 |
| TC-002 | 定级 | 金额>500 | [mock数据] amount=501 | 正常下单 | P1 | UI显示: 无(纯接口);数据结果: 等级置 L2 | 边界：500/501 |
| TC-003 | 定级 | 兜底等级 | [mock数据] amount=100 | 正常下单 | P2 | UI显示: 无(纯接口);数据结果: 等级置 L1 | else 兜底分支 |

- [ ] **Step 6: 改写场景 4（嵌套 if）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 创建订单 | 不使用优惠券 | [mock数据] couponId=null | 不传 couponId | P1 | UI显示: 无(纯接口);数据结果: 跳过优惠券逻辑 | 外层假分支 |
| TC-002 | 创建订单 | 优惠券无效 | [mock数据] couponId!=null 且券不存在 | 传 {couponId:999} | P1 | UI显示: 无(纯接口);数据结果: 返回错误"优惠券无效" | 外层真+内层真；代码摘录 |
| TC-003 | 创建订单 | 优惠券已失效 | [mock数据] couponId!=null、券存在但非 ACTIVE | 传失效券 | P1 | UI显示: 无(纯接口);数据结果: 返回错误"优惠券已失效" | 外层真+内层 else-if 真 |
| TC-004 | 创建订单 | 优惠券有效 | [mock数据] couponId!=null、券存在且 ACTIVE | 传有效券 | P1 | UI显示: 无(纯接口);数据结果: 折扣置为券面额，下单成功 | 外层真+内层全假；推断 |

- [ ] **Step 7: 改写场景 5（switch / case / default）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 创建订单 | 微信支付 | [mock数据] 前置校验全过 | 传 payType=1 | P1 | UI显示: 无(纯接口);数据结果: payChannel=wechat | case 1；代码摘录 |
| TC-002 | 创建订单 | 支付宝支付 | [mock数据] 前置校验全过 | 传 payType=2 | P1 | UI显示: 无(纯接口);数据结果: payChannel=alipay | case 2 |
| TC-003 | 创建订单 | 银联支付 | [mock数据] 前置校验全过 | 传 payType=3 | P1 | UI显示: 无(纯接口);数据结果: payChannel=unionpay | case 3 |
| TC-004 | 创建订单 | 支付方式不支持 | [mock数据] 前置校验全过 | 传 payType=99 | P2 | UI显示: 无(纯接口);数据结果: 返回错误"支付方式不支持" | default；代码摘录 |

- [ ] **Step 8: 改写场景 6（复合条件 && / ||）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 查询订单 | id为空校验 | [mock数据] 调用查询接口 | 传 id=null | P0 | UI显示: 无(纯接口);数据结果: 返回错误"订单ID不合法" | 原子1(id==null)不满足 |
| TC-002 | 查询订单 | id非正数校验 | [mock数据] 调用查询接口 | 传 id=0 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"订单ID不合法" | 原子2(id<=0)不满足；边界 |
| TC-003 | 查询订单 | id合法-正常 | [mock数据] id>0 | 正常查询 | P1 | UI显示: 无(纯接口);数据结果: 继续执行 | 成功路径 |

- [ ] **Step 9: 改写场景 7（三目运算符）模板表（页面展示场景）**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 展示标签 | VIP标签 | [页面触发] 已登录进入页面，isVip=true | 进入页面 | P1 | UI显示: 显示"VIP";数据结果: 无(纯展示) | 三目真分支 |
| TC-002 | 展示标签 | 普通标签 | [页面触发] 已登录进入页面，isVip=false | 进入页面 | P1 | UI显示: 显示"普通用户";数据结果: 无(纯展示) | 三目假分支 |

- [ ] **Step 10: 改写场景 8（循环内条件 + break/continue）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 下单结算 | 数量不合法 | [mock数据] 购物车含 quantity<=0 | 提交订单 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"商品数量不合法" | 循环内 if 真；代码摘录 |
| TC-002 | 下单结算 | 商品售罄跳过 | [mock数据] 购物车含售罄商品 | 提交订单 | P1 | UI显示: 无(纯接口);数据结果: 跳过该商品，其余累加 | 循环内 continue |
| TC-003 | 下单结算 | 正常累加 | [mock数据] 购物车含正常商品 | 提交订单 | P1 | UI显示: 无(纯接口);数据结果: total 正确累加 | 循环内条件全假 |
| TC-004 | 下单结算 | 空购物车 | [mock数据] 购物车为空 | 提交订单 | P2 | UI显示: 无(纯接口);数据结果: 返回错误"购物车为空" | 空集合边界 |

- [ ] **Step 11: 改写场景 9（边界比较）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 创建订单 | 金额超限 | [mock数据] 前置校验合法 | 传 amount=10001 | P1 | UI显示: 无(纯接口);数据结果: 返回错误"单笔订单金额超限" | 主真分支；代码摘录 |
| TC-002 | 创建订单 | 金额边界-等于 | [mock数据] 前置校验合法 | 传 amount=10000 | P2 | UI显示: 无(纯接口);数据结果: 金额校验通过（边界） | 边界=10000 |
| TC-003 | 创建订单 | 金额边界-小于 | [mock数据] 前置校验合法 | 传 amount=9999 | P2 | UI显示: 无(纯接口);数据结果: 金额校验通过 | 边界±1 |
| TC-004 | 创建订单 | 金额为空 | [mock数据] 前置校验合法 | 传 amount=null | P2 | UI显示: 无(纯接口);数据结果: 按空值校验处理 | null 边界 |

- [ ] **Step 12: 改写场景 10（状态机）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 取消订单 | PAID可取消 | [mock数据] 订单状态=PAID（先创建→支付到达） | 取消订单 | P0 | UI显示: 无(纯接口);数据结果: 状态置 CANCELLED | 迁移：PAID→CANCELLED |
| TC-002 | 取消订单 | UNPAID不可取消 | [mock数据] 订单状态=UNPAID | 取消订单 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"当前订单状态不可取消" | 状态取值覆盖 |
| TC-003 | 取消订单 | 已取消重复取消 | [mock数据] 订单状态=CANCELLED | 再次取消 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"当前订单状态不可取消" | 状态取值覆盖 |

- [ ] **Step 13: 改写场景 11（try/catch 异常路径）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 创建订单 | 保存成功 | [mock数据] 依赖正常 | 正常下单 | P1 | UI显示: 无(纯接口);数据结果: 订单保存成功，返回订单 | 正常路径；推断 |
| TC-002 | 创建订单 | 保存失败 | [mock数据] 模拟 DB 异常 | 触发保存异常 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"订单保存失败" | 异常路径；代码摘录 |

- [ ] **Step 14: 改写场景 12（空值/判空校验）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 下单结算 | cart为空对象 | [mock数据] 调用结算接口 | 传 cart=null | P0 | UI显示: 无(纯接口);数据结果: 返回错误"购物车为空" | 原子1(null)不满足 |
| TC-002 | 下单结算 | cart空列表 | [mock数据] cart=[] | 传空数组 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"购物车为空" | 原子2(isEmpty)不满足 |
| TC-003 | 下单结算 | cart非空正常 | [mock数据] cart 含商品 | 正常结算 | P1 | UI显示: 无(纯接口);数据结果: 继续结算 | 成功路径 |

- [ ] **Step 15: 改写场景 13（switch 多 case 合并/类型判断）模板表**

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 消息推送 | 渠道1-微信 | [mock数据] channel=1 | 触发推送 | P1 | UI显示: 无(纯接口);数据结果: 走微信推送 | case 1（与 case 2 合并） |
| TC-002 | 消息推送 | 渠道2-微信 | [mock数据] channel=2 | 触发推送 | P1 | UI显示: 无(纯接口);数据结果: 走微信推送 | case 2 |
| TC-003 | 消息推送 | 其他渠道-短信 | [mock数据] channel=9 | 触发推送 | P2 | UI显示: 无(纯接口);数据结果: 走短信 | default；代码摘录 |

- [ ] **Step 16: 改写综合案例（下单结算）用例清单表**

综合案例表（7 行）改为：

| 用例编号 | 功能 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 下单结算 | 购物车为空对象 | [mock数据] 调用结算接口 | 传 cart=null | P0 | UI显示: 无(纯接口);数据结果: 400"购物车为空" | 复合原子1(null)；代码摘录 |
| TC-002 | 下单结算 | 购物车空列表 | [mock数据] 调用结算接口 | 传 cart=[] | P0 | UI显示: 无(纯接口);数据结果: 400"购物车为空" | 复合原子2(isEmpty)；边界 |
| TC-003 | 下单结算 | 数量不合法 | [mock数据] 购物车含 quantity<=0 | 提交订单 | P0 | UI显示: 无(纯接口);数据结果: 400"商品数量不合法" | 循环内；边界 quantity=0 |
| TC-004 | 下单结算 | 正常累加 | [mock数据] 购物车商品合法 | 提交订单 | P1 | UI显示: 无(纯接口);数据结果: total 正确累加 | 循环内条件全假；推断 |
| TC-005 | 下单结算 | 不用优惠券 | [mock数据] coupon_code 空 | 提交订单 | P1 | UI显示: 无(纯接口);数据结果: 跳过优惠券逻辑 | 嵌套外层假 |
| TC-006 | 下单结算 | 优惠券不存在 | [mock数据] coupon_code 非空、券不存在 | 提交订单 | P1 | UI显示: 无(纯接口);数据结果: 400"优惠券不存在" | 嵌套内层真；代码摘录 |
| TC-007 | 下单结算 | 优惠券有效 | [mock数据] coupon_code 非空、券存在 | 提交订单 | P1 | UI显示: 无(纯接口);数据结果: 走折扣逻辑 | 嵌套内层假；推断 |

- [ ] **Step 17: 验证该文件 46 条全部同步**

Run: `cd /d/AgentDev/code-scenario-testcases && echo "UI显示: $(grep -c 'UI显示:' references/code-snippet-templates.md)"; echo "TC行: $(grep -c '^| TC-' references/code-snippet-templates.md)"; echo "前缀: $(grep -c '\[页面触发\]\|\[mock数据\]' references/code-snippet-templates.md)"`
Expected: `UI显示: 46`；`TC行: 46`；`前缀: 46`

- [ ] **Step 18: Commit**

```bash
cd /d/AgentDev/code-scenario-testcases && git add references/code-snippet-templates.md && git commit -m "feat: v1.20 code-snippet-templates 13 类模板前置条件加触发方式前缀 + 预期结果改 UI显示/数据结果 双部分（46 条）"
```

---

### Task 2: references/input-constraints.md + external-contracts.md 9 条模板同步

**Files:**
- Modify: `references/input-constraints.md`（示例用例表 5 条 + 第 49 行说明）
- Modify: `references/external-contracts.md`（示例用例表 4 条 + 第 25-26 行生成规则 + 第 44 行说明）

**Interfaces:**
- Produces: 两文件共 9 条示例用例行前置含触发方式前缀、预期含 `UI显示:`/`数据结果:` 双部分

- [ ] **Step 1: 改写 input-constraints.md 示例表**

将第 41-47 行示例用例表改为：

| 用例编号 | 用例名称 | 前置条件 | 操作步骤 | 优先级 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|
| TC-001 | userId非空约束 | [mock数据] 调用接口 | 传 userId=null | P0 | UI显示: 无(纯接口);数据结果: 参数校验失败（框架或代码拦截） | 输入约束：@NotNull |
| TC-002 | userId最小值边界 | [mock数据] 调用接口 | 传 userId=0 | P0 | UI显示: 无(纯接口);数据结果: 参数校验失败 | 输入约束：@Min(1)，边界 0/1 |
| TC-003 | skuId非空串约束 | [mock数据] 调用接口 | 传 skuId="" | P0 | UI显示: 无(纯接口);数据结果: 参数校验失败 | 输入约束：@NotBlank |
| TC-004 | amount最大值边界 | [mock数据] 调用接口 | 传 amount=10001 | P1 | UI显示: 无(纯接口);数据结果: 参数校验失败 | 输入约束：@Max(10000)，边界 10000/10001 |
| TC-005 | 合法输入 | [mock数据] 参数全合法 | 传合法值 | P1 | UI显示: 无(纯接口);数据结果: 进入业务逻辑 | 合法等价类 |

- [ ] **Step 2: 改写 input-constraints.md 第 49 行说明**

将第 49 行 `> 说明：约束若由框架层（如 Spring 校验器/Pydantic）拦截，预期结果标"框架校验失败（422/400）"；若代码显式校验，标对应错误文案。` 改为：

```
> 说明：约束若由框架层（如 Spring 校验器/Pydantic）拦截，数据结果标"框架校验失败（422/400）"；若代码显式校验，标对应错误文案。输入约束用例均走接口调用，前置统一标 `[mock数据]`、UI 显示标 `无(纯接口)`。
```

- [ ] **Step 3: 改写 external-contracts.md 生成规则（第 25-26 行）**

将：

```
- 前置条件 = 该依赖被 mock / 返回目标契约状态
- 预期结果 = 代码对该状态的处理（错误串原样摘录 / 推断需联调）
```

改为：

```
- 前置条件 = `[mock数据]` + 该依赖被 mock / 返回目标契约状态
- 预期结果 = `UI显示: 无(纯接口);数据结果: <代码对该状态的处理>`（错误串原样摘录 / 推断需联调）
```

- [ ] **Step 4: 改写 external-contracts.md 示例用例表**

将第 37-42 行示例用例表改为：

| 用例 | 用例名称 | 前置条件 | 预期结果 | 备注 |
|---|---|---|---|---|
| TC-001 | 库存校验-成功 | [mock数据] mock 返回 true | UI显示: 无(纯接口);数据结果: 通过，继续下单 | 外部契约：成功-有效 |
| TC-002 | 库存校验-失败 | [mock数据] mock 返回 false | UI显示: 无(纯接口);数据结果: 返回"库存不足" | 外部契约：成功-失败 |
| TC-003 | 库存校验-抛异常 | [mock数据] mock 抛异常 | UI显示: 无(纯接口);数据结果: 按异常处理/上抛 | 外部契约：抛异常 |
| TC-004 | 库存校验-超时 | [mock数据] mock 超时 | UI显示: 无(纯接口);数据结果: 超时处理（若有） | 外部契约：超时 |

- [ ] **Step 5: 验证两文件 9 条全部同步**

Run: `cd /d/AgentDev/code-scenario-testcases && echo "UI显示: $(grep -c 'UI显示:' references/input-constraints.md references/external-contracts.md | awk -F: '{s+=$2} END {print s}')"; echo "前缀: $(grep -c '\[mock数据\]' references/input-constraints.md references/external-contracts.md | awk -F: '{s+=$2} END {print s}')"`
Expected: `UI显示: 9`；`前缀: 9`

- [ ] **Step 6: Commit**

```bash
cd /d/AgentDev/code-scenario-testcases && git add references/input-constraints.md references/external-contracts.md && git commit -m "feat: v1.20 input-constraints/external-contracts 模板同步触发方式前缀 + 预期结果双部分（9 条）"
```

---

### Task 3: SKILL.md 字段表 + 字段质量自检 12 条 + 输出示例 + 附录模板

**Files:**
- Modify: `SKILL.md`（第 100-103 行措辞、第 112 行前置条件、第 117 行预期结果、第 122-132 行字段质量自检、第 199-205 行附录人工自测模板、第 212-219 行输出示例表）

**Interfaces:**
- Consumes: Task 1/2 的 `[页面触发]`/`[mock数据]` 前缀格式与 `UI显示:`/`数据结果:` 双部分格式
- Produces: skill 运行时会按新格式填写前置条件与预期结果

- [ ] **Step 1: 改写第 3 步字段表「前置条件」行**

将第 112 行：

`| 前置条件 | **可构造的数据/状态前提**（嵌套写外层取值），给具体值（如"存在 id=1 的商品"），**不得以"调用/传"开头**（那是步骤），**不与操作步骤混淆** |`

改为：

`| 前置条件 | **可构造的数据/状态前提**（嵌套写外层取值），给具体值（如"存在 id=1 的商品"），**不得以"调用/传"开头**（那是步骤），**不与操作步骤混淆**；**开头必须标注触发方式**：`[页面触发]`（步骤为页面 UI 操作，前置需真实页面环境）或 `[mock数据]`（步骤为接口调用/mock 依赖/数据预置，前置只靠数据构造） |`

- [ ] **Step 2: 改写第 3 步字段表「预期结果」行**

将第 117 行：

`| 预期结果 | **核心，必须可判定且单一**——精确到返回体/状态码/文案/异常，或给判定标准（如"返回影响行数>0"、"HTTP 200 + data 含商品对象"）。不可判定的"返回商品数据"类描述不合格；错误串原样摘录；备注标注来源（代码摘录 / 推断需联调） |`

改为：

`| 预期结果 | **核心，必须可判定且单一**——统一为 **`UI显示: <页面/客户端现象>;数据结果: <接口/数据/状态变化>`** 双部分：UI 显示写页面/客户端实际看到的（提示文案/弹窗/页面状态/展示值），数据结果写接口返回体/状态码/数据库/缓存变化（精确到返回体/状态码/文案/异常，或判定标准如"返回影响行数>0"）；纯接口用例 UI 显示写 `无(纯接口)`，纯展示用例数据结果写 `无(纯展示)`；不可判定的"返回商品数据"类描述不合格；错误串原样摘录；备注标注来源（代码摘录 / 推断需联调） |`

- [ ] **Step 3: 改写第 3 步「验证方法标注」措辞（第 100 行）**

将第 100 行末句 "**验证方法与预期结果的可观察对象必须匹配**" 保持不动（已覆盖双部分要求）。检查第 103 行字段清单是否需同步——`cases.json` 键名不变（前置/预期仍为单字段），无需改动。

- [ ] **Step 4: 字段质量自检 10 → 12 条**

在字段质量自检第 10 条（场景类型标注正确）之后追加两条：

```
11. **前置条件触发方式标注正确**——前缀 `[页面触发]`/`[mock数据]` 与操作步骤入口方式一致（页面操作→页面触发；接口调用/mock/数据预置→mock数据），不混用不臆测
12. **预期结果双结果完整**——含 `UI显示:` 与 `数据结果:` 两部分；纯接口写 `UI显示: 无(纯接口)`、纯展示写 `数据结果: 无(纯展示)`，不省略任何一部分
```

同时将第 122 行 `**字段质量自检（每条用例生成后必须通过）**` 标题后的条目序号说明同步（若以"第 N 条"引用则不改；若列表序号自动编号则追加即可）。

- [ ] **Step 5: 改写第 7 步发布门禁措辞**

将第 167 行 "**先过发布门禁**：第 3 步的 10 条字段质量自检逐条复查（尤其预期可判定、前置不写成操作、可观察对象与验证方法匹配）" 改为：

```
**先过发布门禁**：第 3 步的 12 条字段质量自检逐条复查（尤其预期可判定、前置带触发方式前缀、预期 UI显示/数据结果 双部分完整、可观察对象与验证方法匹配）
```

- [ ] **Step 6: 改写附录「人工自测 8 列模板」示例与纪律**

将第 199-205 行附录中示例表的 3 行示例改为：

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 验证方法 | 优先级 | 结果 |
|---|---|---|---|---|---|---|---|
| TC-ORD-001 | 下单-正常流程 | [mock数据] 登录态有效，库存=5 | 提交订单，数量=1 | UI显示: 无(纯接口);数据结果: HTTP 200，返回订单号，库存变 4 | 接口响应+查库 | P0 | PASS |
| TC-ORD-002 | 下单-库存不足 | [mock数据] 登录态有效，库存=1 | 提交订单，数量=2 | UI显示: 无(纯接口);数据结果: 返回"库存不足"，订单不生成 | 接口响应 | P0 | FAIL |
| ... | | | | | | | |

并将第 205 行纪律句中的 "预期结果可判定且单一（精确到文案/状态码）" 改为 "预期结果统一为 `UI显示:...;数据结果:...` 双部分且可判定（精确到文案/状态码）；前置条件带 `[页面触发]`/`[mock数据]` 前缀"。

- [ ] **Step 7: 改写输出示例表（第 212-219 行）**

将示例表 4 行改为：

| 用例编号 | 功能 | 用例名称 | 场景类型 | 验证点 | 前置条件 | 操作步骤 | 验证方法 | 可自动化程度 | 优先级 | 预期结果 | 代码位置 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-ORD-001 | 创建订单 | 用户为空校验 | 异常流程 | 必填：userId 非空（业务规则） | [mock数据] 创建订单接口可用，无历史订单依赖 | POST /order，body 传 {userId:null} | 接口响应 | 可单测 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"用户不能为空"，不生成订单 | OrderService.java:3 |
| TC-ORD-002 | 创建订单 | 库存不足校验 | 异常流程 | 库存规则（外部契约） | [mock数据] userId 有效；mock 库存校验返回 false | POST /order，body 传 {stock:false} | 接口响应 | 可单测 | P0 | UI显示: 无(纯接口);数据结果: 返回错误"库存不足" | OrderService.java:5 |
| TC-ORD-003 | 创建订单 | 微信支付 | 正常流程 | 支付方式选择（判定点） | [mock数据] userId 有效，库存充足 | POST /order，传 payType=1 | 数据状态 | 可单测 | P1 | UI显示: 无(纯接口);数据结果: 订单 payChannel 置为 wechat（查库确认） | OrderService.java:8 |
| TC-ORD-004 | 创建订单 | 支付方式不支持 | 异常流程 | 兜底分支（判定点） | [mock数据] userId 有效 | POST /order，传 payType=3 | 接口响应 | 可单测 | P2 | UI显示: 无(纯接口);数据结果: 返回错误"支付方式不支持" | OrderService.java:11 |

- [ ] **Step 8: 验证 SKILL.md 改动一致**

Run: `cd /d/AgentDev/code-scenario-testcases && echo "12条: $(grep -c '前置条件触发方式标注正确' SKILL.md)"; echo "双结果: $(grep -c '预期结果双结果完整' SKILL.md)"; echo "UI显示: $(grep -c 'UI显示:' SKILL.md)"; echo "前缀: $(grep -c '\[页面触发\]\|\[mock数据\]' SKILL.md)"; echo "残留10条: $(grep -n '10 条字段质量自检' SKILL.md)"`
Expected: `12条: 1`；`双结果: 1`；`UI显示: ≥4`（字段表+自检12+附录示例+输出示例表）；`前缀: ≥2`；`残留10条: 无输出`

- [ ] **Step 9: Commit**

```bash
cd /d/AgentDev/code-scenario-testcases && git add SKILL.md && git commit -m "feat: v1.20 SKILL.md 前置条件触发方式前缀 + 预期结果 UI显示/数据结果 双部分 + 自检 12 条"
```

---

### Task 4: README.md 同步

**Files:**
- Modify: `README.md`（§1.1 能力表第 26 行预期结果、§1.2 主表列说明第 69/74 行、§2.3 cases.json 示例第 212/217 行、§2.4 关键设计决策、更新日志）

**Interfaces:**
- Consumes: Task 3 的 `[页面触发]`/`[mock数据]` 前缀与 `UI显示:`/`数据结果:` 双部分格式
- Produces: README 与实现一致的 v1.20 描述

- [ ] **Step 1: §1.1 能力表「预期结果」行同步**

将第 26 行：

`| **预期结果** | 不通过/校验失败分支写出功能"展示的结果"（错误码+文案、提示、跳转、异常）；错误串**原样摘录**；每条预期结果标注来源（代码摘录 / 推断需联调） |`

改为：

`| **预期结果** | 统一为 **`UI显示:`（页面/客户端现象） + `数据结果:`（接口/数据/状态变化）** 双部分，纯接口写 `无(纯接口)`、纯展示写 `无(纯展示)`；错误串**原样摘录**；每条预期结果标注来源（代码摘录 / 推断需联调） |`

在 §1.1 能力表「场景类型标注」行之后追加一行：

`| **触发方式标注** | 每条用例**前置条件**以 `[页面触发]`/`[mock数据]` 开头明确触发方式（页面 UI 操作 vs 接口/mock/数据预置），执行者可据此确定自测环境（页面环境 or 数据构造） |`

- [ ] **Step 2: §1.2 主 sheet 列说明两行同步**

将第 69 行：

`| 前置条件 | 触发该分支所需的数据/状态前提（不得写成操作步骤） |`

改为：

`| 前置条件 | 以 `[页面触发]`/`[mock数据]` 开头标注触发方式 + 数据/状态前提（页面操作→页面触发；接口/mock/数据预置→mock数据；不得写成操作步骤） |`

将第 74 行：

`| 预期结果 | **核心字段**，不通过分支写功能展示的结果，必须可判定且单一 |`

改为：

`| 预期结果 | **核心字段**，统一为 `UI显示:`（页面/客户端现象）+ `数据结果:`（接口/数据/状态变化）双部分，纯接口/纯展示显式标注，必须可判定且单一 |`

- [ ] **Step 3: §2.3 cases.json 示例两行同步**

将第 212 行 `"precondition": "创建订单接口可用，无历史订单依赖",` 改为：

```jsonc
      "precondition": "[mock数据] 创建订单接口可用，无历史订单依赖",   // 触发方式: 页面触发 / mock数据
```

将第 217 行 `"expected_result": "返回错误\"用户不能为空\"",` 改为：

```jsonc
      "expected_result": "UI显示: 无(纯接口);数据结果: 返回错误\"用户不能为空\"",  // UI显示 + 数据结果 双部分
```

- [ ] **Step 4: §2.4 关键设计决策加一行**

在 §2.4 表格末尾（「场景类型显式标注」行之后）追加：

```markdown
| **前置触发方式 + 预期双结果标注** | 前置条件用 `[页面触发]`/`[mock数据]` 明确触发方式（页面环境 vs 数据构造），预期结果强制分离 `UI显示:`（界面现象）与 `数据结果:`（落库/接口状态），研发自测可区分"界面对了数据错"与"数据对了界面没展示"两类缺陷 |
```

- [ ] **Step 5: 更新日志加 v1.20**

在 v1.19 行之后追加：

```markdown
| **v1.20** | 前置触发方式 + 预期双结果 | 前置条件列强制以 `[页面触发]`/`[mock数据]` 开头明确触发方式；预期结果列强制分离 `UI显示:`（页面/客户端现象）与 `数据结果:`（接口/数据/状态变化），纯接口/纯展示显式标注；字段质量自检 10→12 条；references 模板全部同步新格式（code-snippet-templates 13 类 + input-constraints + external-contracts） |
```

- [ ] **Step 6: 验证 README 改动一致**

Run: `cd /d/AgentDev/code-scenario-testcases && echo "UI显示: $(grep -c 'UI显示:' README.md)"; echo "前缀: $(grep -c '\[页面触发\]\|\[mock数据\]' README.md)"; echo "v1.20: $(grep -c 'v1.20' README.md)"`
Expected: `UI显示: ≥3`；`前缀: ≥3`；`v1.20: ≥2`（更新日志 + §2.4 决策行）

- [ ] **Step 7: Commit**

```bash
cd /d/AgentDev/code-scenario-testcases && git add README.md && git commit -m "docs: v1.20 README 同步触发方式前缀 + 预期结果双部分 + 更新日志"
```

---

### Task 5: 终验（全量回归 + 部署一致性）

**Files:**
- Verify: `references/code-snippet-templates.md`、`references/input-constraints.md`、`references/external-contracts.md`、`SKILL.md`、`README.md`、运行时 `C:\Users\46018\.claude\skills\code-scenario-testcases\SKILL.md`

**Interfaces:**
- Consumes: Task 1-4 全部改动

- [ ] **Step 1: 全量 grep 回归（55 条模板）**

Run: `cd /d/AgentDev/code-scenario-testcases && for f in references/code-snippet-templates.md references/input-constraints.md references/external-contracts.md; do echo "$f UI显示:$(grep -c 'UI显示:' $f) 前缀:$(grep -c '\[页面触发\]\|\[mock数据\]' $f) TC行:$(grep -c '^| TC-' $f)"; done`
Expected: 每个文件 `UI显示:` 与 `前缀:` 均等于其 `TC行:`（code-snippet-templates 46/46/46、input-constraints 5/5/5、external-contracts 4/4/4），合计 55/55/55

- [ ] **Step 2: SKILL/README 一致性**

Run: `cd /d/AgentDev/code-scenario-testcases && grep -n "10 条字段质量自检\|9 条\|14 列" SKILL.md README.md`
Expected: 无输出（历史更新日志中的 `10→12`、`14→15` 等箭头表述除外）

- [ ] **Step 3: 确认运行时已部署**

Run: `diff <(sed -n '1,999p' /d/AgentDev/code-scenario-testcases/SKILL.md) /c/Users/46018/.claude/skills/code-scenario-testcases/SKILL.md && echo SAME`
Expected: `SAME`（若 Task 3/4 的 commit 均已触发 install.sh）

- [ ] **Step 4: 若 Step 3 不一致，手动部署**

Run: `cd /d/AgentDev/code-scenario-testcases && ./install.sh`
Expected: 输出 `install: skill 已部署到 /c/Users/46018/.claude/skills/code-scenario-testcases`，重跑 Step 3 确认 SAME

- [ ] **Step 5: （可选人工）用真实 skill 跑 order-java 抽查**

在 Claude Code 中调用 `code-scenario-testcases -all`（目录 `D:/AgentDev/fixtures/order-java`），抽查生成 Excel：主 sheet 前置条件列均以 `[页面触发]`/`[mock数据]` 开头、预期结果列均含 `UI显示:` 与 `数据结果:` 两部分。此步为人工冒烟，不阻塞验收。

---

## 自审结论

- **Spec 覆盖**：前置条件触发方式前缀 → Task 1 各场景 + Task 2 + Task 3 Step 1；预期结果双部分 → Task 1 各场景 + Task 2 + Task 3 Step 2；自检 12 条 → Task 3 Step 4-5；附录/输出示例 → Task 3 Step 6-7；README 同步 → Task 4；55 条模板回归 → Task 5 Step 1-2。
- **占位符扫描**：无 TBD/TODO；每个改写目标均给出完整表格内容，执行者照抄即可。
- **类型一致性**：`[页面触发]`/`[mock数据]` 前缀与 `UI显示:`/`数据结果:` 双部分格式在三处（Task 1/2 模板、Task 3 SKILL、Task 4 README）表述一致；`cases.json` 键名（precondition/expected_result）未变。
